{
	"$schema": "https://turbo.build/schema.json",
	"extends": ["//"],
	"tasks": {
		"build": {
			"outputs": [".next/**"],
			"inputs": ["app/**", "package.json", "tsconfig.json", "next.config.ts"]
		}
	}
}
